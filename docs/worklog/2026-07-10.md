# RhythMark 作業ログ — 2026-07-10

## サマリー

Claude Code運用ルール(CLAUDE.md)を整備しコミット(Phase A完了)。App Store申請準備の棚卸しを実施し、TestFlight配布(Phase B-1)の全体計画を確定。次回はApp Store Connect側の手作業(Step 1)の結果報告から再開する。

## 1. Phase A: Claude Code運用整備(コミット `73ad297`)

- **CLAUDE.md作成**: サブエージェント委譲ルール(設定ファイル検証は json-validator、定型実装は implementer、タイマー精度・オーディオセッション・バックグラウンド動作の設計変更はメインセッション)とプロジェクト前提を記載
- **json-validator動作確認**: app.json / eas.json / package.json の整合性検証を2回実施(ローカル版・origin/main版)。構文・整合性エラー0件。Bundle ID法人名義統一(`com.murakamiworks.rhythmark`)が両OSで一致していることを確認
- **ローカル10コミット遅延の発見と解消**: ローカルmainがorigin/mainより10コミット遅れていた(Bundle ID変更・プリセット機能・プライバシーページ追加等を未取得)。`git pull --ff-only` でfast-forward解消。再発防止としてCLAUDE.mdに「セッション開始時は git fetch && git status で差分確認」ルールを追記

## 2. App Store申請準備の棚卸し結果

- **完了済み**: Bundle ID法人名義変更(実機ビルド確認済み)、プライバシー/サポートページ公開(https://www.murakamiworks.co.jp/apps/rhythmark/privacy /support)、6.9インチスクリーンショット6枚(1320×2868、仕様適合)、Apple Developer Program(法人)契約・支払い完了
- **未着手**: App Store ConnectでのApp作成・Bundle ID登録、eas.json の submit.production 設定(現状空)、初回本番ビルド・提出、TestFlight配布
- 07-04のworklog以降の続報がリポジトリになかったため、Developer Program承認状況はユーザーへの確認で判明(契約済み・TransHougenで使用中)

## 3. Phase B-1: TestFlight配布の全体計画

| Step | 担当 | 内容 |
| --- | --- | --- |
| 1 | ユーザー(コンソール) | developer.apple.com でBundle ID登録(Explicit、Capability追加不要)→ ASCでApp作成(名前RhythMark、日本語、SKU rhythmark)→ Apple ID(数字)を控える。スクリーンショット/URL登録は内部TestFlightのブロッカーではないため待ち時間に実施可 |
| 2 | Claude | 報告値で eas.json の submit.production.ios(appleId / ascAppId / appleTeamId)を設定、json-validator検証後コミット |
| 3 | Claude+ユーザー | `npx eas-cli build --platform ios --profile production`。認証ポイント: ①Expoログイン ②Apple ID+2FA(初回の配布証明書生成)。ビルド番号はEAS自動採番(appVersionSource: remote) |
| 4 | Claude+ユーザー | `npx eas-cli submit --platform ios --latest`。認証ポイント: ③ASC API Key生成(初回、推奨)。提出後5〜30分でTestFlightタブに出現 |
| 5 | ユーザー(コンソール+iPhone) | TestFlightタブで内部グループ作成→自分をテスター追加→ビルド割当→iPhoneのTestFlightアプリからインストール。有効期限90日 |

## 4. 承認済み事項

- **`ITSAppUsesNonExemptEncryption: false` の追加**(app.json の ios.infoPlist): ビルドごとの輸出コンプライアンス手動回答を省略するため。Step 2で実施
- **privacy.html文言修正の implementer 委譲**: 「個人情報を収集しません」→「いかなるデータも収集しません」(英語も同様に "any data" へ)、最終更新日を2026-07-10に更新。ASCの「データ収集なし」申告と平仄を合わせる
  - 注意: リポジトリ修正だけでは公開中の法人サイトに反映されない。反映経路の確認・反映作業が別途必要

## 次のアクション

1. **ユーザー**: Step 1(Bundle ID登録・App作成)を実施し、以下3つの値を報告
   - Team ID(英数10桁、developer.apple.com → Membership details)
   - ascAppId(ASCのApp情報ページの「Apple ID」、数字)
   - Apple Accountメール(eas submit の appleId 用)
2. **Claude**: 報告を受けて Step 2(eas.json設定 + ITSAppUsesNonExemptEncryption追加)から再開
3. 並行タスク: privacy.html文言修正を implementer に委譲(承認済み、いつでも実施可)
